//// Get the connection
//var conn = DDP.connect('http://localhost:3333');
//
//// Pass the connection to `DDP.loginWithPassword`, which is otherwise similar to
//// the core `Meteor.loginWithPassword` method.
//DDP.loginWithPassword(conn, {username: 'mda'}, 'test', function (error) {
//    if (!error) {
//        console.log("Logged in!");
//
//    } else {
//        console.log(error);
//    }
//});
